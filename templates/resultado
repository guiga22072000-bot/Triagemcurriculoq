<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Resultado da Análise</title>
  <link rel="stylesheet" href="{{ url_for('static', filename='style.css') }}">
</head>
<body>
  <div class="container wide">
    <header>
      <div class="logo-wrapper">
        <img src="{{ url_for('static', filename='logo.jpeg') }}" alt="SODITRI.IA">
      </div>
      <h1>Resultado da Análise</h1>
      <a href="/" class="back-link">← Nova análise</a>
    </header>

    <details class="job-profile-box">
      <summary>Ver perfil da vaga utilizado</summary>
      <p>{{ job_profile }}</p>
    </details>

    <form action="/exportar" method="post">
      <input type="hidden" name="results_json" value='{{ results | tojson | safe }}'>
      <button type="submit" class="export-btn">⬇️ Exportar para Excel (.xlsx)</button>
    </form>

    <div class="table-wrapper">
      <table>
        <thead>
          <tr>
            <th>Nome do Candidato</th>
            <th>WhatsApp</th>
            <th>Email</th>
            <th>Compatibilidade</th>
            <th>Status</th>
            <th>Justificativa</th>
            <th>Arquivo</th>
          </tr>
        </thead>
        <tbody>
          {% for r in results %}
          <tr class="{% if 'não' in (r.status|lower) or 'nao' in (r.status|lower) %}row-no{% elif 'recomendado' in (r.status|lower) %}row-yes{% endif %}">
            <td>{{ r.nome }}</td>
            <td>{{ r.whatsapp }}</td>
            <td>{{ r.email }}</td>
            <td class="score-cell">{{ r.score }}{% if r.score is number or (r.score|string).isdigit() %}%{% endif %}</td>
            <td><span class="status-badge">{{ r.status }}</span></td>
            <td class="justificativa">{{ r.justificativa }}</td>
            <td class="filename">{{ r.arquivo }}</td>
          </tr>
          {% endfor %}
        </tbody>
      </table>
    </div>
  </div>
</body>
</html>
